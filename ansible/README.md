


## OAI Config

arfcn SSB must fit in a grid of `3000 MHz * N * 1.44 MHz`
absolute frequency point A must be multiple of 12 below that


`Invalid CSET0 start PRB -12 SSB offset point A 4 RB offset 16` => add -12 * 12 (*2) to current Point A; no idea where the *2 comes from though...
